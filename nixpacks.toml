[phases.setup]
nixPkgs = ["nodejs", "nodePackages.npm", "python311", "python311Packages.pip"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "npm run prod"
