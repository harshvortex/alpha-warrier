[phases.setup]
nixPkgs = ["nodePackages.npm", "python311", "nodejs"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "npm run prod"
